const assert = require('assert');

// const ToTest = require(pathToTest);

/*
describe('description du test', () => {
    it('dois retourne X', () => {
           assert.equal(question, reponse);
       });
   });
*/

describe('description du test', () => {
    it('dois retourne X', () => {
           assert.equal(true, true);
       });
   });
